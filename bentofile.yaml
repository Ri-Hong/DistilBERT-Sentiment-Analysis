service: "app.service:SentimentService"
labels:
  owner: mlops-team
  stage: production
include:
  - "app/**"
  - "model/**"
  - "requirements.txt"
python:
  packages:
    - "bentoml>=1.4.0"
    - "transformers>=4.40.0"
    - "torch"
    - "pydantic>=1.10,<3"

# Remove the entire `docker:` section for now
